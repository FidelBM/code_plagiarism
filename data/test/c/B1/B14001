#include<queue>
#include<set>
#include<deque>
#include<stack>
#include<bitset>
#include<algorithm>
#include<functional>
#include<numeric>
#include<utility>
#include<sstream>
#include<iostream>
#include<iomanip>
#include<cstdio>
#include<cmath>
#include<cstdlib>
#include<ctime>
#include<cstring>
#include<vector>
#include<algorithm>
#include<cstdlib>
#include<ctime>
#include<cstring>
#include<map>
#include<fstream>
using namespace std;
#define dbg(x) cout << #x << " -> " << (x) << "\t";
#define rep(i,n) for(int i=0;i<n;i++)
#define pb push_back
#define sz size()
#define psi pair<string,int>
#define all(x) x.begin(),x.end()
#define GI ({int t;scanf("%d",&t); t;})
#define flush(x) memset(x,0,sizeof(x))
#define ll long long
#define mod 1000000007
#define maxn 1000010
#define GLI ({ll t;scanf("%lld",&t); t;})
#define tr(i,a) for( typeof(a.begin()) i=a.begin();i!=a.end();i++)
int f(int n, int m) {
  int res = 0;
  for(int i = n; i <= m; i++) {
    stringstream ss;
    ss<<i;
    string s = ss.str();
    rep(j,s.sz) {
      string s1 = s.substr(0,j), s2 = s.substr(j);
      if(s1 == "" || s2 == "") continue;
      string temp = ""; temp += s2; temp += s1;
      stringstream ss1(temp);
      int curr;
      ss1>>curr;
      if(curr >= n && curr <= m && temp != s)  res++;
      
    }
  }
  return res/2;
}
int main() {
  int t = GI, kase = 1, T;
  T = t;
  while(t--) {
    int lo, hi;
    lo = GI; hi = GI;
    cout<<"Case #"<<kase<<": "<<f(lo,hi);
    if(kase != T) cout<<endl;
    kase++;
  }
}