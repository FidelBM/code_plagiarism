#include <iostream>
#include <stdio.h>
#include <math.h>
#include <algorithm>
#include <map>
#define eps 1e-9
#define inf 0x7fffffff
#define N 1005
using namespace std;
int main()
{
    int n,i,j,m=0,t;
    //freopen ("3.out","w",stdout);
    cin>>t;
    for (j=0;j<t;j++){
    int a,b;
    m=0;
    cin>>a>>b;
    int s=1,d=b,c=0;
    while (d/=10){c++;s*=10;}
    s*=10;
    for (i=a;i<=b;i++){
        int y=10,x=0,z=c;
        bool k[200001]={0};
        while (z--){
              int p,q;
              p=i%y*(s/y);
              q=i/y;
              if (p+q<=b && p+q>i && p+q>s/10 && !k[p+q]){x++;k[p+q]=1;
              //cout<<i<<" "<<p+q<<endl;
              }
              y*=10;
              }
        m+=x;
        }
    cout<<"Case #"<<j+1<<": "<<m<<endl;
    }
    /*cin>>t;
    for (j=0;j<t;j++){
        int a,b;
        cin>>a>>b;
        
        cout<<"Case #"<<j+1<<": "<<m<<endl;
        }*/
    return 0;
}
