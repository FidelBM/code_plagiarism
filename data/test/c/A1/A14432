#ifndef TESTOBJ_H
#define TESTOBJ_H

#include <iostream>
#include <fstream>

class TestObj
{
    public:
        TestObj();
        virtual ~TestObj();
        void Run();
    protected:
        virtual const char* InputFileName() = 0;
        virtual void Process(std::ifstream& in, std::ofstream& out) = 0;
    private:
};

#endif // TESTOBJ_H
